# Current build status

The Phase 2 line now contains a reproducible x86_64 UEFI PC development image builder, a guarded whole-disk installer, label-based early userspace discovery, and the typed runtime contract.

The CI workflow is responsible for producing the `luna-pc-x86_64` artifact when this line is evaluated against `main`.
