[package]
name = "luna-root-mapping"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
luna-fs = { path = "../luna-fs" }
libc = { version = "0.2", optional = true }

[features]
default = ["linux-backend"]
linux-backend = ["dep:libc"]
